
/*
import 'mocha';
import { expect } from 'chai';
import { algoritmosGenericos } from '../src/modif';

describe('prueba modif', () =>{
  let numero: number = 2
  let numeroFiltro: number = 2
  let array: number[] = [1, 4, 6, 8, 10];
  let algoritm = new algoritmosGenericos([1, 4, 6, 8, 10], 2);
  let arrayResultFilter: number[] = [6, 8, 10]
  let arrayResultMap: number[] = [1, 16, 36, 64, 100]

  it('test de filter', () => {
    expect(algoritm.run(functions).to.be.deep.equal(arrayResultFilter);
  });

  it('test de map', () => {
    expect(array.map(array)).to.be.deep.equal(arrayResultMap);
  });

  it('test de reduce', () => {
    expect(array.reduce(array)).to.be.equal(29);
  });
})
*/